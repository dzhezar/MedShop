<div class="page_top">
    <ul class="breadcrumb">
        {% if breadcrumbs is defined %}
            {% for breadcrumb in breadcrumbs %}
                <li><a href="{{ breadcrumb.url }}">{{ breadcrumb.name }}</a></li>
            {% endfor %}
        {% endif %}
    </ul>
    <div class="product-title">QQQQQQ</div>
</div>
