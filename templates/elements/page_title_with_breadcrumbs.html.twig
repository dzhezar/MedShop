<div class="page_top">
    {% if breadcrumbs is defined %}
    <ul class="breadcrumb">
        {% if breadcrumbs is defined %}
            {% for breadcrumb in breadcrumbs %}
                <li><a href="{{ breadcrumb.url }}">{{ breadcrumb.name }}</a></li>
            {% endfor %}
        {% endif %}
    </ul>
        <div class="product-title">{{ breadcrumbs|last.name }}</div>
    {% endif %}
</div>
